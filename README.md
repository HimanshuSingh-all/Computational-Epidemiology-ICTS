Dependency: THis will require pyenv if you want to copy and run everything as is
```
pyenv install 3.12.1
pyenv local 3.12.1
python -m venv env
source env/bin/activate
pip install -r requirements.txt
```
